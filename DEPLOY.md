# Деплой Kinemon Games на внешний сервер

## Вариант 1: Render.com (Рекомендуется - БЕСПЛАТНО)

### Шаг 1: Подготовка GitHub репозитория

1. **Инициализируйте Git** (если еще не сделали):
```bash
git init
git add .
git commit -m "Initial commit - Kinemon Games v3.0"
```

2. **Создайте репозиторий на GitHub**:
   - Перейдите на https://github.com/new
   - Название: `kinemon-games` (или любое другое)
   - Сделайте репозиторий **Public** или **Private**

3. **Загрузите код на GitHub**:
```bash
git remote add origin https://github.com/ВАШ_ЛОГИН/kinemon-games.git
git branch -M main
git push -u origin main
```

### Шаг 2: Деплой на Render.com

1. **Зарегистрируйтесь на Render.com**:
   - Перейдите на https://render.com
   - Нажмите "Get Started" → войдите через GitHub

2. **Создайте новый Web Service**:
   - На главной странице нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий
   - Выберите репозиторий `kinemon-games`

3. **Настройте сервис**:
   - **Name**: `kinemon-games` (или любое имя)
   - **Environment**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Plan**: `Free` (бесплатный план)
   - Нажмите "Create Web Service"

4. **Дождитесь деплоя** (2-3 минуты):
   - Render автоматически установит зависимости и запустит сервер
   - Вы получите URL вида: `https://kinemon-games.onrender.com`

### Шаг 3: Получение WebSocket URL

После успешного деплоя ваш сервер будет доступен по адресу:
```
wss://kinemon-games.onrender.com
```

**ВАЖНО**: Используйте `wss://` (защищенный WebSocket), а не `ws://`!

### Шаг 4: Обновление клиентских файлов

Откройте файлы на вашем компьютере и измените URL сервера:

**В `display.html` (строка 269):**
```javascript
serverUrlInput.value = `wss://kinemon-games.onrender.com`;
```

**В `controller.html` (строка 271):**
```javascript
serverUrlInput.value = `wss://kinemon-games.onrender.com`;
```

### Шаг 5: Хостинг статических файлов (HTML)

Теперь нужно разместить HTML файлы, чтобы к ним можно было обращаться через интернет.

#### Вариант A: GitHub Pages (самый простой)

1. В вашем GitHub репозитории перейдите в **Settings** → **Pages**
2. Source: выберите **Deploy from a branch**
3. Branch: выберите **main** → папка **/ (root)**
4. Нажмите **Save**
5. Через 1-2 минуты сайт будет доступен по адресу:
   ```
   https://ВАШ_ЛОГИН.github.io/kinemon-games/
   ```

#### Вариант B: Vercel (альтернатива)

1. Перейдите на https://vercel.com
2. Войдите через GitHub
3. Нажмите "Add New..." → "Project"
4. Выберите репозиторий `kinemon-games`
5. Нажмите "Deploy"
6. Получите URL: `https://kinemon-games.vercel.app`

### Шаг 6: Сканирование QR-кода камерой

После деплоя:

1. **На компьютере** откройте:
   ```
   https://ВАШ_ЛОГИН.github.io/kinemon-games/display.html
   ```

2. Нажмите "Подключиться" → появится QR-код

3. **На телефоне** откройте обычную камеру:
   - **iPhone**: Откройте приложение "Камера", наведите на QR-код
   - **Android**: Откройте камеру или Google Lens
   - Нажмите на уведомление о ссылке

4. Автоматически откроется `controller.html` с уже заполненными данными комнаты!

---

## Вариант 2: Railway.app (альтернатива Render)

### Преимущества Railway:
- Более стабильный бесплатный план
- Не засыпает как Render

### Инструкция:

1. **Перейдите на https://railway.app**
2. Войдите через GitHub
3. Нажмите "New Project" → "Deploy from GitHub repo"
4. Выберите `kinemon-games`
5. Railway автоматически определит Node.js проект
6. После деплоя перейдите в **Settings** → скопируйте URL
7. Используйте `wss://ВАШ-ПРОЕКТ.up.railway.app`

---

## Вариант 3: Glitch.com (самый простой, но медленнее)

### Без GitHub:

1. Перейдите на https://glitch.com
2. Нажмите "New Project" → "Import from GitHub"
3. Вставьте URL вашего репозитория
4. Glitch автоматически запустит сервер
5. Получите URL: `wss://ваш-проект.glitch.me`

---

## Важные замечания

### 1. Бесплатные ограничения Render.com:
- Сервер **засыпает** после 15 минут бездействия
- Первое подключение может занять 30-60 секунд (сервер просыпается)
- Ограничение: 750 часов в месяц (достаточно для тестирования)

### 2. Как избежать засыпания (опционально):
Создайте файл `keep-alive.js`:
```javascript
const https = require('https');
setInterval(() => {
    https.get('https://kinemon-games.onrender.com', (res) => {
        console.log('Keep-alive ping');
    });
}, 14 * 60 * 1000); // Каждые 14 минут
```

### 3. HTTPS и WSS:
- Render автоматически предоставляет HTTPS/WSS
- GitHub Pages тоже работает только через HTTPS
- Это важно для работы камеры (требует HTTPS)

---

## Проверка деплоя

После завершения всех шагов проверьте:

1. ✅ Сервер работает: откройте `https://kinemon-games.onrender.com` в браузере
   - Должна появиться главная страница (index.html)

2. ✅ WebSocket работает: откройте консоль браузера (F12)
   - Не должно быть ошибок подключения

3. ✅ QR-код генерируется: откройте display.html
   - Должен появиться QR-код с данными комнаты

4. ✅ Камера сканирует QR: наведите камеру телефона
   - Должна появиться ссылка на controller.html

---

## Что дальше?

После успешного деплоя:

- **Поделитесь ссылкой**: отправьте друзьям URL игры
- **Играйте онлайн**: подключайте несколько телефонов одновременно
- **QR-код работает**: сканируйте обычной камерой без приложений

Готово! Теперь ваша игра доступна из любой точки мира 🎮🌐
