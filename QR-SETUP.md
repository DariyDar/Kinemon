# Настройка QR-кода для сканирования камерой телефона

## Как это работает

1. **На компьютере**: Открываете `display.html` → появляется QR-код
2. **На телефоне**: Наводите камеру на QR-код → появляется ссылка
3. **Нажимаете на ссылку** → автоматически открывается `controller.html` с заполненными данными

## Требования

### Для сканирования QR-кода камерой нужен HTTPS!

❌ **НЕ РАБОТАЕТ**: Открытие файлов локально (`file:///`)
✅ **РАБОТАЕТ**: Хостинг через HTTPS (`https://...`)

### Варианты хостинга с HTTPS:

1. **GitHub Pages** (рекомендуется):
   - Бесплатно
   - Автоматический HTTPS
   - URL: `https://username.github.io/kinemon-games/`

2. **Vercel**:
   - Бесплатно
   - Автоматический HTTPS
   - URL: `https://kinemon-games.vercel.app`

3. **Netlify**:
   - Бесплатно
   - Автоматический HTTPS
   - URL: `https://kinemon-games.netlify.app`

## Настройка GitHub Pages (самый простой способ)

### Шаг 1: Загрузите код на GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/kinemon-games.git
git push -u origin main
```

### Шаг 2: Включите GitHub Pages

1. Перейдите в репозиторий на GitHub
2. **Settings** → **Pages**
3. Source: **Deploy from a branch**
4. Branch: **main** → папка **/ (root)**
5. Нажмите **Save**

### Шаг 3: Дождитесь деплоя (1-2 минуты)

Сайт будет доступен по адресу:
```
https://YOUR_USERNAME.github.io/kinemon-games/
```

### Шаг 4: Проверьте QR-код

1. Откройте `https://YOUR_USERNAME.github.io/kinemon-games/display.html`
2. Нажмите "Подключиться"
3. Появится QR-код
4. Наведите камеру iPhone/Android на QR-код
5. Должна появиться ссылка на `controller.html`

## Как работает QR-код

QR-код содержит JSON с данными:

```javascript
{
  "roomId": "ABC123",
  "serverUrl": "wss://kinemon-games.onrender.com"
}
```

Когда вы сканируете QR-код, браузер видит JSON и не знает, что с ним делать.

### Решение: Deep Link

Нужно создать специальную ссылку вместо JSON:

```
https://YOUR_USERNAME.github.io/kinemon-games/controller.html?room=ABC123&server=wss://kinemon-games.onrender.com
```

## Обновление кода для Deep Link

Я обновлю файлы, чтобы QR-код содержал прямую ссылку вместо JSON.

### В display.html - генерация QR с URL:

```javascript
// Вместо JSON
const qrData = JSON.stringify({
    roomId: roomId,
    serverUrl: serverUrlInput.value
});

// Используем прямую ссылку
const baseUrl = window.location.origin + window.location.pathname.replace('display.html', 'controller.html');
const qrData = `${baseUrl}?room=${roomId}&server=${encodeURIComponent(serverUrlInput.value)}`;
```

### В controller.html - чтение параметров:

```javascript
// Проверяем URL параметры
const urlParams = new URLSearchParams(window.location.search);
const roomFromUrl = urlParams.get('room');
const serverFromUrl = urlParams.get('server');

if (roomFromUrl && serverFromUrl) {
    // Автоматически заполняем и подключаемся
    roomIdInput.value = roomFromUrl;
    serverUrlInput.value = serverFromUrl;
    connectToRoom(roomFromUrl, serverFromUrl);
}
```

Хотите, чтобы я обновил код для поддержки Deep Link?

## Альтернатива: Использование приложения QR-сканера

Если не хотите использовать Deep Link, можно:

1. Установить приложение для сканирования QR-кодов (например, "QR Code Reader")
2. Приложение покажет JSON текст
3. Вручную скопировать `roomId` и `serverUrl`
4. Открыть `controller.html` и вставить данные

Но это неудобно. Лучше использовать Deep Link.

## Проверка работы камеры

### iPhone:
- Откройте приложение "Камера"
- Наведите на QR-код
- Должно появиться уведомление вверху экрана
- Нажмите на уведомление

### Android:
- Откройте камеру или Google Lens
- Наведите на QR-код
- Нажмите на всплывающую кнопку

### Если камера не распознает:
1. Убедитесь, что QR-код четкий и крупный
2. Убедитесь, что у камеры включено распознавание QR-кодов (настройки камеры)
3. Попробуйте приложение "QR Code Reader"

## Итоговая схема работы

```
┌──────────────┐
│  Компьютер   │
│  (Display)   │
│              │
│ QR-код:      │
│ https://.../ │
│ controller   │
│ ?room=ABC123 │
│ &server=wss..│
└──────────────┘
       │
       │ Камера сканирует
       ▼
┌──────────────┐
│   Телефон    │
│              │
│ Уведомление: │
│ "Открыть     │
│ ссылку?"     │
└──────────────┘
       │
       │ Нажимаем
       ▼
┌──────────────┐
│ Controller   │
│ автоматически│
│ подключается │
│ к комнате    │
│ ABC123       │
└──────────────┘
```

Готово! Теперь можно играть просто отсканировав QR-код 🎮
