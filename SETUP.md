# Kinemon Games - Quick Setup Guide

## Оффлайн режим (без сервера)

1. Откройте `index.html` в браузере на телефоне
2. Выберите "Оффлайн"
3. Выберите игру и играйте!

---

## Онлайн режим (с сервером)

### Шаг 1: Установка зависимостей

```bash
npm install
```

### Шаг 2: Запуск сервера

```bash
npm start
```

Сервер запустится на порту 8080. Вы увидите:
```
Kinemon Games server running on http://localhost:8080
```

### Шаг 3: Узнайте IP адрес вашего компьютера

**Windows:**
```bash
ipconfig
```
Найдите "IPv4 Address" (например: 192.168.1.100)

**Mac/Linux:**
```bash
ifconfig
```
Найдите "inet" адрес (например: 192.168.1.100)

### Шаг 4: Подключение контроллера (телефон)

1. На телефоне откройте: `http://[IP-адрес]:8080`
2. Выберите "Онлайн" → "Контроллер"
3. Введите имя игрока
4. Введите адрес сервера: `ws://[IP-адрес]:8080`
   - Пример: `ws://192.168.1.100:8080`
5. Нажмите "Подключиться"
6. Пройдите калибровку

### Шаг 5: Подключение экрана (компьютер)

1. На компьютере откройте: `http://localhost:8080`
2. Выберите "Онлайн" → "Экран"
3. Введите адрес сервера: `ws://localhost:8080`
4. Нажмите "Подключиться"
5. Наблюдайте за игрой всех подключенных игроков!

---

## Проблемы и решения

### Телефон не подключается к серверу

- Убедитесь, что телефон и компьютер в одной Wi-Fi сети
- Проверьте, что сервер запущен (`npm start`)
- Проверьте правильность IP адреса
- Используйте `ws://` (не `wss://` или `http://`)

### Сервер не запускается

- Проверьте, что Node.js установлен: `node --version`
- Установите зависимости: `npm install`
- Проверьте, что порт 8080 свободен

### Калибровка не работает

- Разрешите доступ к датчикам движения в браузере
- Используйте Safari или Chrome на iOS
- Убедитесь, что телефон не заблокирован

---

## Дополнительные команды

### Изменить порт сервера

```bash
PORT=3000 npm start
```

### Запуск в production режиме

```bash
NODE_ENV=production npm start
```

---

**Готово!** Теперь вы можете играть в многопользовательский режим 🎮
