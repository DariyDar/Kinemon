# Kinemon Games v3.0 - Сводка изменений

## ✅ Что сделано

### 1. Подготовка к деплою в интернет

**Обновленные файлы:**
- ✅ `package.json` - добавлен engines для Node.js 14+
- ✅ `.gitignore` - добавлены IDE папки
- ✅ `display.html` - автоопределение localhost/production
- ✅ `controller.html` - автоопределение localhost/production

### 2. QR-код для сканирования камерой телефона

**Как работает:**
1. На компьютере открываете `display.html`
2. Появляется QR-код с прямой ссылкой:
   ```
   https://username.github.io/kinemon-games/controller.html?room=ABC123&server=wss://...
   ```
3. Наводите камеру телефона на QR-код
4. Нажимаете на уведомление → автоматически открывается `controller.html` с заполненными данными!

**Изменения в коде:**

**display.html:**
- Генерация QR-кода теперь создает прямую ссылку вместо JSON
- URL формата: `https://.../controller.html?room=ABC123&server=wss://...`

**controller.html:**
- Автоматическое чтение параметров из URL (`URLSearchParams`)
- Автозаполнение полей комнаты и сервера
- Автоматическое подключение при переходе по ссылке
- Удалена библиотека html5-qrcode (больше не нужна)
- Удален экран QR-сканера (используется встроенная камера)

### 3. Документация

Созданы файлы:
- ✅ `DEPLOY.md` - полная инструкция по деплою (Render.com, Railway, Glitch)
- ✅ `QUICKSTART.md` - быстрый старт за 5 минут
- ✅ `QR-SETUP.md` - настройка QR-кода
- ✅ `README_GITHUB.md` - красивый README для GitHub
- ✅ `.env.example` - пример конфигурации
- ✅ `update-server-url.js` - скрипт для обновления URL
- ✅ `SUMMARY.md` - этот файл

### 4. Скрипты для удобства

**package.json:**
```json
"scripts": {
  "start": "node server.js",
  "update-url": "node update-server-url.js"
}
```

**Использование:**
```bash
# Обновить URL сервера во всех файлах
npm run update-url wss://your-project.onrender.com
```

---

## 📋 Что нужно сделать для запуска в интернете

### Шаг 1: Загрузить код на GitHub (2 минуты)

```bash
git init
git add .
git commit -m "Kinemon Games v3.0 - Online Multiplayer with QR"
git remote add origin https://github.com/YOUR_USERNAME/kinemon-games.git
git branch -M main
git push -u origin main
```

### Шаг 2: Деплой сервера на Render.com (3 минуты)

1. Перейдите на https://render.com
2. Войдите через GitHub
3. New + → Web Service
4. Выберите репозиторий `kinemon-games`
5. Настройки:
   - Name: `kinemon-games`
   - Environment: Node
   - Build: `npm install`
   - Start: `npm start`
   - Plan: Free
6. Create Web Service
7. **Скопируйте URL** (например: `https://kinemon-games.onrender.com`)

### Шаг 3: Обновить URL в коде (1 минута)

**Вариант A: Автоматически**
```bash
npm run update-url wss://kinemon-games.onrender.com
git add .
git commit -m "Update server URL"
git push
```

**Вариант B: Вручную**

В `display.html` (строка 271):
```javascript
const defaultServer = isLocalhost ? 'ws://localhost:8080' : 'wss://kinemon-games.onrender.com';
```

В `controller.html` (строка 273):
```javascript
const defaultServer = isLocalhost ? 'ws://localhost:8080' : 'wss://kinemon-games.onrender.com';
```

### Шаг 4: Деплой HTML на GitHub Pages (1 минута)

1. В GitHub: **Settings** → **Pages**
2. Source: **Deploy from a branch**
3. Branch: **main** → **/ (root)**
4. Save

Через 2 минуты доступно по адресу:
```
https://YOUR_USERNAME.github.io/kinemon-games/
```

---

## 🎮 Как играть после деплоя

### На компьютере:
1. Откройте: `https://YOUR_USERNAME.github.io/kinemon-games/display.html`
2. Нажмите "Подключиться"
3. Появится QR-код и код комнаты

### На телефоне:
1. **Вариант A**: Откройте камеру → наведите на QR-код → нажмите уведомление ✨
2. **Вариант B**: Откройте `controller.html` → введите код комнаты вручную

### Калибровка и игра:
1. Пройдите калибровку (2 шага)
2. Управляйте змейкой наклоном телефона
3. Собирайте пиццу и избегайте столкновений!

---

## 🔧 Технические детали

### Автоопределение окружения

Код автоматически определяет, где запущен:

```javascript
const isLocalhost = window.location.hostname === 'localhost'
    || window.location.hostname === '127.0.0.1'
    || window.location.hostname === '';

const defaultServer = isLocalhost
    ? 'ws://localhost:8080'              // Локально
    : 'wss://kinemon-games.onrender.com'; // В продакшене
```

### Формат QR-кода

**Старый (не работает с камерой):**
```json
{"roomId":"ABC123","serverUrl":"wss://..."}
```

**Новый (работает с камерой):**
```
https://username.github.io/kinemon-games/controller.html?room=ABC123&server=wss%3A%2F%2F...
```

### Чтение параметров из URL

```javascript
const urlParams = new URLSearchParams(window.location.search);
const roomFromUrl = urlParams.get('room');       // "ABC123"
const serverFromUrl = urlParams.get('server');   // "wss://..."

if (roomFromUrl && serverFromUrl) {
    // Автоматически заполняем и подключаемся
    connectToRoom(roomFromUrl, serverFromUrl);
}
```

---

## 📊 Структура проекта

```
kinemon-games/
├── 📄 index.html              # Главное меню (оффлайн/онлайн)
├── 🖥️ display.html            # Игровой дисплей + QR-код
├── 📱 controller.html         # Контроллер с автоподключением
├── ⚙️ server.js               # WebSocket сервер
├── 🎮 motionController.js     # Device Orientation API
├── 🎯 calibration.js          # Калибровка наклона
├── 🏓 pongGame.js             # Игра Pong
├── 🐍 slitherGame.js          # Игра Snake
├── 📦 package.json            # Зависимости
├── 📖 README.md               # Основная документация
├── 📖 README_GITHUB.md        # README для GitHub
├── 📖 DEPLOY.md               # Инструкции по деплою
├── 📖 QUICKSTART.md           # Быстрый старт
├── 📖 QR-SETUP.md             # Настройка QR-кода
├── 📖 SUMMARY.md              # Эта сводка
├── 🔧 update-server-url.js    # Скрипт обновления URL
└── 🙈 .gitignore              # Игнорируемые файлы
```

---

## ✨ Новые возможности v3.0

### Система комнат
- ✅ Уникальный 6-значный код для каждой игры
- ✅ Множество независимых комнат на одном сервере
- ✅ Автоматическое удаление пустых комнат

### QR-код сканирование
- ✅ Генерация QR-кода с прямой ссылкой
- ✅ Сканирование встроенной камерой (без приложений!)
- ✅ Автоматическое подключение при сканировании
- ✅ Fallback на ручной ввод кода

### Мультиплеер
- ✅ Несколько игроков на одном экране
- ✅ Синхронизация в реальном времени (60 FPS)
- ✅ PvP столкновения
- ✅ Таблица лидеров

### Деплой
- ✅ Готово к деплою на Render.com / Railway / Glitch
- ✅ Автоопределение localhost/production
- ✅ Поддержка HTTPS/WSS
- ✅ Скрипт для обновления URL

---

## 🚀 Следующие шаги

1. **Деплой** - следуйте инструкциям в `QUICKSTART.md`
2. **Тестирование** - проверьте QR-код сканирование
3. **Настройка** - измените URL сервера на свой
4. **Игра** - зовите друзей и играйте!

---

## 🐛 Известные проблемы

### Render.com засыпает
**Проблема**: Бесплатный сервер спит после 15 минут
**Решение**: Первое подключение занимает 30-60 секунд

### iOS требует HTTPS для камеры
**Проблема**: Камера не работает на `http://`
**Решение**: Используйте GitHub Pages (автоматически HTTPS)

### QR-код не сканируется
**Проблема**: Камера не распознает QR-код
**Решение**:
1. Проверьте, что файлы на HTTPS
2. Используйте ручной ввод кода
3. Попробуйте приложение "QR Code Reader"

---

## 📞 Помощь

**Полезные ссылки:**
- [DEPLOY.md](./DEPLOY.md) - подробная инструкция по деплою
- [QUICKSTART.md](./QUICKSTART.md) - быстрый старт
- [QR-SETUP.md](./QR-SETUP.md) - настройка QR-кода

**Troubleshooting:**
1. Сервер не запускается → проверьте `npm install`
2. QR-код не работает → проверьте HTTPS
3. Контроллер не подключается → проверьте URL сервера
4. Калибровка не работает → дайте разрешение на датчики

---

**Готово! Игра готова к запуску в интернете 🎉**
